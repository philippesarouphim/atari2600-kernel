Initialization:

    ; SET VARIABLES
    lda #$9E
    sta COLUBK

    lda #$58
    sta COLUPF

    lda #%00000000
    sta CTRLPF

    lda #$54
    sta COLUP0
    lda #$D6
    sta COLUP1

    lda #80
    sta $81